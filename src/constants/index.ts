export const APPLICATION_ELEMENT_ID = "root";

/** 空值占位符 */
export const EMPTY_PLACE_HOLDER = "";

/** 时间格式化方式 */
export const FORMAT = "YYYY-MM-DD HH:mm:ss";

/** 是 */
export const TRUE = 1;

/** 否 */
export const FALSE = 0;

/** 超级管理员id，不可操作 */
export const SUPER_ADMIN_ID = 1;

/** 自定义的iconfont名称集合 */
export const iconfontTypes = [
  "sentimental-octocat",
  "sentimental-full-screen",
  "sentimental-reload",
  "sentimental-setting",
  "sentimental-right",
  "sentimental-user",
  "sentimental-close",
  "sentimental-down",
  "sentimental-duigou",
  "sentimental-chahao"
] as const;
